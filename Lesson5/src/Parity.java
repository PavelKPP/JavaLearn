import java.util.Scanner;

public class Parity {
    public static void main(String[] args) {
      /*
        int x = 5;
        int y = 10;
        int z = 15;

        x += y >> x++ * z;
        System.out.println("Result of x += y >> x++ * z :" + x);
        z = ++x & y * 5;
        System.out.println("Result of z = ++x & y * 5 :" + z);
        y /= x + 5 | z;
        System.out.println("Result of y /= x + 5 | z :" + y);
        z = x++ & y * 5;
        System.out.println("Result of z = x++ & y * 5 :" + z);
        x = y << x++ ^ z;
        System.out.println("Result of x = y << x++ ^ z :" + x);

       */
        /*System.out.println("Put your woking expirience in years: ");
        int years;
        Scanner sc = new Scanner(System.in);
        years = sc.nextInt();
        int premium;
        int salary = 22500;

        if (years >= 0 && years < 5){
            premium = (int) (salary * 0.1);
            System.out.println(premium);
        }
        if (years >= 5 && years < 10){
            premium = (int) (salary * 0.15);
            System.out.println(premium);
        }
        if (years >= 10 && years < 15){
            premium = (int) (salary * 0.25);
            System.out.println(premium);
        }
        if (years >= 15 && years < 20){
            premium = (int) (salary * 0.35);
            System.out.println(premium);
        }
        if (years >= 20 && years < 25){
            premium = (int) (salary * 0.45);
            System.out.println(premium);
        }
        if (years >= 25){
            premium = (int) (salary * 0.5);
            System.out.println(premium);
        }
*/
        boolean a;
        boolean b;

        if (a | b)
            System.out.println("A | B" + (a | b));
        else
            System.out.println("A | B" + (a | b));




    };
}